ANTHROPIC_API_KEY=sk-ant-your-key-here

# GitHub organization for project repos (default: cordada)
GITHUB_ORG=cordada

# Directory where project repos are cloned (default: ./projects)
# PROJECTS_DIR=/path/to/projects
